# AI Voicebot Feedback Page

## 🚀 How to Use

### 1. Frontend
- Open `index.html` in your browser.
- Click the "🎤 Talk to AI Assistant" button to speak with the voicebot.

### 2. Backend (Node.js)
- Install dependencies:
  ```
  npm install express openai cors
  ```

- Replace `YOUR_OPENAI_API_KEY` in `server.js` with your real OpenAI key.

- Start the server:
  ```
  node server.js
  ```

- Make sure it's running on `http://localhost:3000`.

### ✅ Done!
The voicebot will listen to you, send your message to OpenAI, and speak back the answer.
